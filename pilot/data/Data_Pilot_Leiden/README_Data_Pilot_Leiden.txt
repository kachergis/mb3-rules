The relevant columns for looking times during test are:

`LookTime2.Block.`,`LookTime4.Block.`,  `LookTime6.Block.`, etc (only even numbers). The number of total look times depends on the number of separate looks that infants made.